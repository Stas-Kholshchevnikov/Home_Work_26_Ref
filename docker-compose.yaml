version: "3.3"

services:
  app:
    build: .
    ports:
      - 5000:5000
    depends_on:
      - db

  db:
    image: postgres:latest
    environment:
      - POSTGRES_USER=db_user
      - POSTGRES_PASSWORD=db_password
      - POSTGRES_DB=db_name
    ports:
      - 5436:5432




